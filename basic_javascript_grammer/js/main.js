'use strict';

{
  const scores = [80, 90, 40];
  // console.log(scores[1]);

  // scores[2] = 44;
  // console.log(scores);

  // constの注意点:scoresそのものへの再代入はできないが、配列の要素そのものへの再代入は可能

  console.log(scores.length);
}